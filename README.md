# Mix.js

### Getting started

##### Install Node dependencies
`npm install`
#### Setup bower
`npm run install-bower`
##### Build & Test
`npm test` or `grunt`
##### Start a local webserver at `http://localhost:8888`
`npm start`
